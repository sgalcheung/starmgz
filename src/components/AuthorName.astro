---
import type { Author } from '../data/author';

interface Props{
  author: Author;
}

const {author} = Astro.props;

---

{author.bioUrl ? (
  <a href={author.bioUrl} target="_blank" rel="noopener noreferrer" class="author-link">
    {author.name}
  </a>
) : (
  author.name
)}
